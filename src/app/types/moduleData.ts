export interface ModuleData{
  moduleId:number,
  moduleName:string,
  categoryName : string,
  categoryId : number,
  categoryStatus: boolean,
  active : boolean;
  updatedAt : number,
  comments ?: string,
}
